import { ArtisanNamePipe } from './artisan-name.pipe';

describe('ArtisanNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtisanNamePipe();
    expect(pipe).toBeTruthy();
  });
});
