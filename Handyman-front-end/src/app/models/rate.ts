export class Rate {
  id_artisan: number;
  username_client: string;


  constructor(id_artisan: number, username_client: string) {
    this.id_artisan = id_artisan;
    this.username_client = username_client;
  }
}
